<UserControl x:Class="Chrome.Views.ShellUserControls.TopBarUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Height="43"
             DataContext="{Binding}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Border x:Name="PART_ChromeTitleBar"
                Grid.Row="0"
                Grid.Column="0"
                HorizontalAlignment="Stretch"
                Background="{StaticResource DefaultBackground}"
                BorderBrush="Transparent"
                BorderThickness="1"
                CornerRadius="4"
                Margin="1">
            <Grid HorizontalAlignment="Stretch">
                <TextBlock VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Margin="15,0,0,0"
                           Foreground="#fff">
                    METRO CRM Light
                </TextBlock>

            </Grid>
        </Border>

        <StackPanel Grid.Row="0"
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Margin="1">
            <Border BorderBrush="Transparent"
                    Background="{StaticResource DefaultBackground}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Width="38"
                    Margin="0,0,1,0"
                    Padding="0,0,0,5"
                    ToolTip="Minimieren"
                    Cursor="Hand">
                <materialDesign:PackIcon Kind="WindowMinimize"
                                         Foreground="White"
                                         VerticalAlignment="Bottom"
                                         HorizontalAlignment="Center" />
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonUp">
                        <i:InvokeCommandAction
                            Command="{Binding ViewModel.MinimizeShellCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Border>
            <Border BorderBrush="Transparent"
                    Background="{StaticResource DefaultBackground}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Width="38"
                    Margin="0,0,2,0"
                    Padding="1"
                    ToolTip="Maximieren"
                    Cursor="Hand">
                <materialDesign:PackIcon Kind="WindowMaximize"
                                         Foreground="White"
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Center" 
                                         Width="25" Height="25"/>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonUp">
                        <i:InvokeCommandAction
                            Command="{Binding ViewModel.MaximizeShellCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Border>
            <Border BorderBrush="Transparent"
                    Background="{StaticResource DefaultBackground}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Width="38"
                    Margin="0"
                    Padding="1"
                    ToolTip="Schließen"
                    Cursor="Hand">
                <materialDesign:PackIcon Kind="WindowClose"
                                         Foreground="White"
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Center"
                                         Width="25"
                                         Height="25" />
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonUp">
                        <i:InvokeCommandAction
                            Command="{Binding ViewModel.CloseShellCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Border>
        </StackPanel>

    </Grid>
</UserControl>