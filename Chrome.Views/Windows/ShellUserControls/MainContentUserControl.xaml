<UserControl x:Class="Chrome.Views.Windows.ShellUserControls.MainContentUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext="{Binding}"
             xmlns:uebersicht="clr-namespace:Chrome.Views.Windows.Uebersicht">
    <!--<ContentPresenter
        Content="{Binding ViewModel.CurrentUserControl, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch" />-->

    <ContentControl HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">

        <Canvas MouseMove="CanvasMain_OnMouseMove"
                MouseUp="CanvasMain_OnMouseUp"
                Background="DimGray">
            <uebersicht:UbersichtAktion Canvas.Top="50"
                                   Canvas.Left="50"
                                   Width="600"
                                   Height="400" />
        </Canvas>

    </ContentControl>

</UserControl>